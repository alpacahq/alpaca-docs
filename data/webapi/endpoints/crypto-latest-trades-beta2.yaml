endpoints:
  /v1beta2/crypto/latest/trades:
    GET:
      title: Returns latest trade for the queried crypto symbols
      summary: |
        This endpoint returns latest trade for the requested crypto symbol.
      params:
        query:
          - name: symbols
            type: string
            required: true
            desc: Comma separated list of symbols.
      returns: |
        A Trade response object.